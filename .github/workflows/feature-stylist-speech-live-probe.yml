name: K Scan Staging Stylist Speech Live Probe (feature)

on:
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: kscan-build32-speech-control-diagnostics
  cancel-in-progress: false

jobs:
  live-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: staging
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '20'
      - name: Recover delayed diagnostics for the bounded control probe
        env:
          SUPABASE_STAGING_PROJECT_REF: ${{ vars.SUPABASE_STAGING_PROJECT_REF }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: node security/release/query-build32-speech-control-diagnostics.js > build32-speech-control-diagnostics.json
      - name: Print sanitized evidence
        if: always()
        run: test -s build32-speech-control-diagnostics.json && cat build32-speech-control-diagnostics.json
      - name: Upload sanitized evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: build32-speech-control-diagnostics-${{ github.run_id }}
          path: build32-speech-control-diagnostics.json
          retention-days: 30
