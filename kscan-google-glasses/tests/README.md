# Tests

## Phone bridge / shared contract

```bash
cd phone-bridge
npm install
npm test
```

## Android

- Unit tests for `VoiceCommandController` and `FocusNavigator` — TODO Prompt 2
- Instrumented Compose tests — TODO Prompt 2

## Mock images

Place non-sensitive test JPEGs in `tests/mock-images/` for manual analyze runs.
Do not commit images containing identifiable faces.
