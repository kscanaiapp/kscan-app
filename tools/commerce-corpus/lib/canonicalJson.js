'use strict';

/**
 * Reused directly (not reimplemented) from the Match Quality Lab — see
 * tools/commerce-corpus/lib/privacyGuard.js for the reuse rationale. Pure,
 * generic, deterministic-serialization utilities; no Match-Quality-specific
 * coupling.
 */
module.exports = require('../../fashion-match-quality/lib/canonicalJson');
