# Governance mismatch proof

This file exists only on a controlled, non-mergeable-by-policy proof branch. The branch intentionally starts before the master governance bootstrap, so Git's predicted merge tree includes base-branch controls that are absent from the head tree. `Master promotion tree equivalence` must fail, and this branch must never merge.
