# 07 — Database migration record

- File: `supabase/migrations/20260722000001_stylechat_request_quota_events.sql`
- Applied to project `wyyuqfdxucjksghsmhry` via Supabase MCP `apply_migration` (`stylechat_request_quota_events`) — **success**
- Security: SECURITY DEFINER, `search_path=public`, `auth.uid()` gated, execute granted to `authenticated` only; table not writable by clients
