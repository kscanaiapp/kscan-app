package com.kscan.glasses.config

/**
 * Named feature flags for beta toggles.
 */
enum class BetaFeatureFlag {
    MOCK_BRIDGE,
    MOCK_API,
    MOCK_SUPABASE,
    REAL_ANALYZE,
    REAL_CONNECTIVITY,
    REAL_VOICE,
    REAL_CAMERA,
    REAL_FACE_MASKING,
}
