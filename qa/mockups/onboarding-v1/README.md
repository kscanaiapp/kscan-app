# K Scan AI — Onboarding V1 Mockups

These images are visual references only.

Do not import them into app code.

Do not copy them into production assets.

Do not use them as runtime images.

Do not commit them unless explicitly approved.

If the mockups conflict with the written build prompt, the written prompt wins.

Privacy, permissions, auth behavior, and data behavior must follow the build prompt, not the mockup visuals.

The Home mockup is a future Home V2 polish reference only. Do not add fake commerce, fake prices, fake brands, fake products, fake match percentages, or fake inventory during the onboarding framework build.
